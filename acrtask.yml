version: v1.1.0

steps:
- build: -t {{.Run.Registry}}/loxone-influx:latest -t {{.Run.Registry}}/loxone-influx:$(date+"%Y%m%d") .
- push: 
    - $Registry/loxone-influx:latest
    - $Registry/loxone-influx:$(date +"%Y%m%d")
